{
  "name": "@gagg/providers-meta",
  "version": "0.1.0",
  "private": true,
  "description": "Providers de metadata: RAWG (nota da comunidade + Metacritic), Steam (reviews %), agregação de scores.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  }
}
