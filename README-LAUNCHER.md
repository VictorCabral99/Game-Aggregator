# Game Aggregator (web — branch `main`)

Cópia isolada do Next.js que roda em `main` (0830ab6), separada do launcher Electron em `c:\Projetos\game-aggregator` (`develop`).

- Porta do atalho: **http://localhost:3000**
- Subir: atalho da área de trabalho **Game Aggregator**, ou `scripts\start-game-aggregator.vbs`
- Env: `.env` / `.env.local` (NEXTAUTH_URL já aponta para 3000)
- Worktree git: `git -C c:\Projetos\game-aggregator worktree list`
